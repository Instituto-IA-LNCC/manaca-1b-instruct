# Ambiente local e artefatos de execução (não versionar)
# (ancorados na raiz: NAO afetam docs/data, que e documentacao do corpus)
.env
/data/
/hf-cache/
/checkpoints/
__pycache__/
*.pyc

# Config accelerate gerado em runtime pelo run_sft.sh
configs/.accelerate_*.gen.yaml

# Conteudo nocivo cru NAO versionado (obediencia do modelo / pares DPO de seguranca):
bench/safety_pt/answers/
/dpo_onpolicy/
sft_safety/

